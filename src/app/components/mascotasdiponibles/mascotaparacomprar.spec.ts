import { Mascotaparacomprar } from './mascotaparacomprar';

describe('Mascotaparacomprar', () => {
  it('should create an instance', () => {
    const directive = Mascotaparacomprar;
    expect(directive).toBeTruthy();
  });
});
